---
topic: Wire real Topic suggestions in Step 3, with LLM-invented new topics support (Topic.status pending/approved/rejected)
updated: 2026-07-30T21:19
---

- (note) create from _bmad-output/implementation-artifacts/deferred-work.md's 2026-07-28 intent-clarification entry (already-confirmed design, not yet spec'd) plus direct codebase reading (Topic/Source models, sources.py, preferences.py, admin.py, admin_taxonomy.py); express distill, no elicitation; slug topic-suggestions
- (decision) user explicitly approved the FULL feature now (migration + admin approval flow + LLM inventing new topic names), not an interim subscribed-only-merge version - asked directly, chose full scope
- (capability) CAP-1 Step 3 suggestion set merges approved-Topic candidates the LLM ranks as relevant with brand-new Topic names the LLM invents from the user's Field/Role/Interest-Free-Text, so the grid is never just an echo of the user's existing subscriptions
- (capability) CAP-2 selecting a not-yet-existing suggested Topic immediately creates a real Topic row (status=pending, get-or-create by exact name like add_topic today) plus a real UserTopicPreference for the selecting user - fully functional for them right away, no admin gate blocks their own use
- (capability) CAP-3 only status=approved Topics are ever offered as suggestion candidates to OTHER users; a pending Topic is invisible to everyone except the user who created it until an admin decides it
- (capability) CAP-4 admin can approve or reject a pending Topic via a dedicated review endpoint+UI mirroring Source approval (admin.py pattern, not admin_taxonomy.py's PendingTaxonomySuggestion pattern, since Topic status lives on the Topic row itself); approve flips to approved (now suggestible to others); reject flips to rejected permanently, no un-reject
- (capability) CAP-5 existing Topics migrate cleanly: Alembic migration adds status column defaulting every pre-existing row to approved, so current seeded/admin-added Topics keep working exactly as before with zero behavior change
- (constraint) Topic selection stays a hard FK (UserTopicPreference.topic_id) - no free-text Other mechanism like Role/Field; a new name MUST become a real Topic row before anything can reference it, which is why this differs from PendingTaxonomySuggestion's defer-the-row-until-promotion pattern
- (constraint) MAX_TOPICS=4 selection cap (preferences.py) is unchanged and still enforced by the existing set_preferences/TopicCapExceededError path when a newly-created pending topic is among the picks
- (constraint) suggest_topics's LLM adapter contract must return two distinguishable kinds of result - existing-candidate picks by topic_id, and new-topic proposals by name - so the service layer can route each to the right write path (a plain topic_id list can no longer represent the full contract)
- (constraint) _topic_popularity's candidate list (services/profile.py) must filter to status=approved only and must now carry Topic.name alongside topic_id+selection_count - the LLM cannot reason about or avoid duplicating topics it cannot see the name of
- (constraint) a user's own preferences view (list_topic_choices / GET /me/preferences) must keep showing any pending or rejected Topic they personally are already subscribed to, by name - status only gates whether a Topic is offered as a suggestion to OTHERS, never whether the owning user can see/keep their own pick
- (constraint) new-Topic creation reuses add_topic's existing get-or-create-by-exact-name idempotency unchanged (strip whitespace only) - no fuzzy/normalized matching introduced for Topics; that would be new scope beyond this feature
- (constraint) suggestions/ stays DB-free (AD-3, same rule as the Role/Prompt story) - services/profile.py queries popularity+approved-topic-names and passes plain data into suggest_topics; the LLM adapter never touches the DB
- (constraint) rejected Topics stay rejected permanently - no re-review/un-reject path, mirrors the deferred design's explicit decision, consistent with how a rejected Source has no un-reject path either
- (assumption) suggestion-grid display cap for merged existing+new Topic candidates set to 10, matching ROLE_SUGGESTION_CAP's established convention in this codebase for consistency - not explicitly stated by the user, flagged as assumption
- (question) OPEN: does clicking a brand-new suggested Topic chip create the Topic row + UserTopicPreference immediately (eager write at click time, before the user hits Save), or only when the user hits Save preferences (batch, like every existing topic toggle today)? The deferred note says 'creates...immediately' but TopicsStep.vue's whole existing UX is pick-then-single-Save with no per-click writes, and pickedIds is currently number[] with no way to represent a not-yet-existing topic by name. This changes whether a new dedicated create-on-click endpoint is needed vs extending the existing PUT /me/preferences payload to accept names. Not resolved - flagged rather than assumed
- (decision) admin Topic review gets its own router file (mirrors admin.py's Source pattern exactly), not an extension of admin_taxonomy.py - follows AD-10's existing precedent that source approval and taxonomy approval are independent concerns that merely share a shape; Topic status lives on the Topic row itself like Source.status, structurally unlike PendingTaxonomySuggestion
- (note) companion topic-status-flow.md spawned to hold the pending/approved/rejected state diagram + transition table (diagrams always land in a companion per Spec Law)
- (decision) non-goals: no re-review/un-reject workflow for rejected Topics; no fuzzy/near-duplicate topic-name merging; no change to Role/Field/PendingTaxonomySuggestion flow (untouched, separate mechanism); no change to MAX_TOPICS cap value; no bulk/batch admin actions (one Topic decided at a time, matches existing Source/Taxonomy admin UX); no proactive admin notification when a new pending Topic appears
- (decision) success signal: pytest suite covers the migration (existing Topics default approved), new-topic get-or-create + immediate UserTopicPreference, approved-only candidate filtering (a pending topic never appears in another user's suggestion set), admin approve/reject transitions (with permanent-rejected verified), and MAX_TOPICS cap enforcement including a newly-created topic. Manual: run the wizard twice with NEWSAGENT_SUGGESTION_PROVIDER=llm as two different profiles, confirm Step 3 shows more than an exact echo of current subscriptions, pick a brand-new topic, save, then as admin see it in the Topic review queue, approve it, and confirm a second differently-profiled user can now also see it suggested
- (event) self-validate pass 1 (coherence): PASS - all 5 CAPs carry intent+success, constraints bend concrete design decisions (FK-vs-freetext, cap filtering, candidate shape, own-preferences visibility, idempotency, DB-free adapters, permanent-rejected), non-goals explicit (6 items), success signal testable (specific pytest+manual checks), IDs unique/stable, prose lean. One open_question retained (eager-write timing) since the input genuinely does not resolve it and inventing an answer would silently fork the API design
- (event) self-validate pass 2 (preservation): PASS - every load-bearing claim from the deferred-work.md design note landed in SPEC.md (status column+migration, immediate row creation, approved-only visibility to others, permanent reject, admin flow needs) or topic-status-flow.md companion (the state transitions themselves); no wrapper-only content dropped
- (event) spec finalized
- (decision) RESOLVED open question: new-Topic row + UserTopicPreference creation happens only at Save preferences time (batch, same as every existing topic toggle today), not eagerly at chip-click time. No dedicated create-on-click endpoint needed - PUT /me/preferences's existing payload/service is extended to accept new-topic names alongside existing topic_ids and resolve/create them server-side during that single save call. User confirmed directly after a clarifying re-ask in plain language
- (event) SPEC.md re-derived: open question resolved and moved into Constraints; no open questions remain. Only the display-cap=10 assumption remains, low-stakes and reasonable
