---
topic: LLM provider interface (issue #5) — contract, pipeline fit, mock provider
goal: understand exactly what to implement before coding
mode: partner
status: complete
updated: 2026-07-19T23:55
---

- (direction) batch composed on page: Role Playing, Reverse Brainstorming, Disney Method, How Might We, Job to Be Done, Morphological Analysis (random), + invent 2, + coach chooses 1
- (technique) started Role Playing
- (idea by user) as reader: refuses boring, hallucinated, or irrelevant articles — summary must be grounded in the real article, relevance bar must be real
- (idea by coach) as Moshe (cost payer): every call reports token cost; per-run spending cap; provider outage must not silently kill the morning digest
- (idea by user) as pipeline dev: uniform interface across providers, clear errors, unified error handling — end user never exposed to raw provider errors
- (idea by coach) as the article itself: half-broken RSS content deserves explicit refusal from the interface, not a forced low-quality summary
- (idea by user) error taxonomy by origin: context/input errors vs LLM/provider errors vs transport/infra errors, each with its own handling policy; caller always sees uniform outcome: success / failure / retry
- (technique) started Reverse Brainstorming
- (idea by user) sabotage: feed broken/nonexistent images → inverse: contract takes clean plain text only, media handling out of provider scope
- (idea by user) sabotage: malicious instructions inside article text (prompt injection) → inverse: article content is data not instructions; adapter must separate instructions from content structurally
- (idea by coach) sabotage: free-form str returns from every method → inverse: structured typed results (dataclass), provider output parsing is adapter-internal
- (idea by coach) sabotage: smart/random mock provider → inverse: mock is deliberately boring, fully deterministic, same input same output
- (technique) started Disney Method
- (idea by user) dream: interface learns reader taste and updates relevance filtering from feedback
- (idea by user) dream: learns user activity, daily schedule, device, favorite articles — adapts behavior per user
- (idea by user) dream: output enriched for clarity/flow/pleasure — ordered, points to sources, notes language and reading time
- (idea by coach) dream: interface as smart router — each call declares task intent, router picks cheapest adequate model per task behind one contract
- (idea by user) realist: score_relevance takes optional user preference history parameter as personalization hook
- (idea by coach) realist: summarize returns structured SummaryResult (summary_he, source language, reading time, translated title) instead of str
- (idea by user) critic: who verifies the summary is actually faithful to the source article? (hallucination verification gap)
- (idea by coach) coach realist adds: usage/token report on every result (covers issue #19); batch scoring with default loop; router dream solved via config (per-task model), not in contract
- (insight) faithfulness verification = optional pipeline stage (cheap heuristic / LLM judge on sample / dogfood human review), contract must not block it — not a provider obligation
- (technique) started How Might We
- (direction) attacking HMW #1: contract stable across provider/model/price churn
- (idea by user) relevance scores must share one calibrated scale across providers — otherwise pipeline thresholds break on swap
- (idea by coach) contract defines scale semantics with anchor points; threshold lives in pipeline config; enforced by abstract contract test suite every adapter (incl mock) must pass
- (technique) started Job to Be Done
- (idea by user) JTBD tests: hired to deliver identical results every run — determinism is the product
- (idea by coach) JTBD pipeline: hired as an editor (trustworthy editorial judgment in Hebrew); JTBD developers: hired to decouple dev velocity from LLM dependency (mock is the product)
- (technique) started Morphological Analysis
- (idea by user) morphological param 5: error handling axis — internal adapter retry vs exposing retry decision to pipeline
- (decision) winning morphological combo A-B-B-C-C: 2 methods; dedicated input dataclass; result dataclass + usage; sync with async door; hybrid retry (adapter retries transient with backoff, then raises typed error)
- (technique) started invented technique 1: The Impostor Adapter — implement the contract with non-LLM impostors (regex, translation API, human, coin flip); wherever the impostor cannot comply, an LLM assumption has leaked into the contract
- (note) invented technique 2 queued: Call-Site First — write the pipeline code that calls the interface before the interface exists; the code you wish you could write dictates the contract
- (idea by user) impostor chosen: database of old summaries — probe where it fails the contract
- (idea by coach) impostor translate-API+keywords exposed leak: usage must be optional/neutral units, token counts are an LLM assumption
- (direction) user calls convergence — skipping remaining techniques (Call-Site First, coach pick), moving to narrow and decide
- (note) convergence move chosen: MoSCoW (scoping a build for issue #5)
- (decision by user) MoSCoW approved as proposed: Must=2 methods, input dataclass, structured results, error hierarchy by origin, hybrid retry, anchored score scale, deterministic mock, contract test suite; Should=optional neutral usage, explicit refusal, data-not-instructions doc; Could=batch, preference-history hook, purity/cache doc; Wont=judge, cost cap, personalization, async, real adapter, router
- (insight) synthesis: (1) trust thread — reader's hallucination fear + critic's verification gap + impostor's refusal = one three-layer answer: grounded clean-text input, explicit refusal channel, optional judge stage later; (2) determinism thread — tests JTBD + cache impostor + boring mock = purity as contract property, enabling legal caching; (3) user's success/failure/retry taxonomy pre-answered the morphological error axis (C); (4) preference-history param is the single point where the personalization dream touches MVP steel
