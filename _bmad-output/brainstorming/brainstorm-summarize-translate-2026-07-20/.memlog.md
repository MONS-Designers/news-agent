---
topic: Summarize+translate stage (issue #11): where summary lives, cost model, reuse of #10 patterns
goal: understand what to implement before coding, short session
mode: autonomous
status: complete
updated: 2026-07-20T01:14
---

- (technique) started First Principles
- (idea by coach) a summary is a fact about the article (like relevance_score), not about a specific user's digest — no user-specific data goes into summarize(); storing it per-DigestArticle would force re-summarizing the same article once per subscribed user, contradicting the #5 purity/once-per-article principle
- (decision) override ISSUES.md's original phrasing ('stored on DigestArticle'): summary_he stored on Article, mirroring relevance_score/relevance_status from #10. DigestArticle (built in #12) references Article and inherits the summary, doesn't own it
- (idea by coach) only articles with relevance_status=relevant should ever be summarized — filtering already paid the cost of deciding that; summarizing irrelevant articles is pure waste
- (idea by coach) state machine mirrors #10 exactly: summary_status pending->summarized|refused(terminal)|error(retryable); only relevant articles enter pending in the first place (set at filter time, not a new default)
- (technique) started Pre-mortem
- (idea by coach) postmortem: digest shipped with garbled/truncated Hebrew because a real provider's summary was silently empty; fix: empty summary_he from a non-refusal result is itself an LLMProviderError-worthy condition the adapter must raise, not a value the pipeline accepts silently
- (idea by coach) postmortem: same article relevant for two topics (rare but possible if a source's topic changes) summarized twice; not a real MVP risk since Article->Source->Topic is fixed 1:1, explicitly noting as non-issue
- (idea by coach) postmortem: schema stores title_he/reading_time/source_language too, matching #5's SummaryResult exactly, so #13 (HTML rendering) never has to recompute or re-fetch them
- (insight) synthesis: stage is a twin of relevance.py — same state machine shape, same per-article commit, same usage aggregation and CLI pattern — but gated on relevance_status=relevant, and persists all 4 SummaryResult fields (summary_he, title_he, source_language, reading_time_minutes) onto Article, not just one score
