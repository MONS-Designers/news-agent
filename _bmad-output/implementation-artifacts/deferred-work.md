# Deferred Work

## Deferred from: spec-gh-39-swappable-log-destination (2026-08-02)

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-39-swappable-log-destination.md`
  summary: The `file` log destination uses a plain `logging.FileHandler` with no rotation, size cap, or retention policy — a long-running deployment with `NEWSAGENT_LOG_LEVEL=DEBUG` will grow the file without bound until the disk fills.
  evidence: Chosen deliberately by the user during spec planning: `RotatingFileHandler` would add two more settings (max bytes, backup count) and amounts to capacity planning GH #39 did not ask for, and the immediate consumer (GH #38's malformed-LLM-output diagnosis) is a short manual run, not a daemon. This becomes a real concern the moment the file destination is enabled on Moshe's server for the scheduled daily pipeline — it belongs with the infra-side log handling in news-agent-infra rather than as an app-level default. Swapping `FileHandler` for `RotatingFileHandler` in `logging_setup.py::_build_handler` is a contained change when that decision is made.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-39-swappable-log-destination.md`
  summary: Destinations that push to an external service (WhatsApp, SMS, email, or a hosted log service) — the user's stated next step for this mechanism — are not implemented; only `stderr`/`stdout`/`file` exist.
  evidence: Named by the user during spec review ("בהמשך, ארצה שזה ישלח לשירות לוגר (ווצאפ, סמס, מייל, או כל אופציה אחרת)"), and deliberately left out of GH #39 whose Acceptance is limited to swapping the destination with no code change. The design already accommodates it: `logging_setup.py::_build_handler` selects a stdlib `logging.Handler` by config value, so a push destination is a new branch returning a custom `Handler` subclass rather than a redesign. Note a push destination has properties a file does not — network failure, rate limits, cost per record, and the need to filter to ERROR+ rather than mirror the root level — so it deserves its own spec rather than an extra `elif`.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-39-swappable-log-destination.md`
  summary: On Windows an open `FileHandler` locks the log file: any attempt to delete or rotate it while the CLI or API process is running fails with `WinError 32`, and two processes appending to one `NEWSAGENT_LOG_FILE` (e.g. the scheduled pipeline plus uvicorn) interleave multi-line tracebacks.
  evidence: Verified during adversarial review of this story: `os.remove` on a file with a live `FileHandler` raises `[WinError 32] The process cannot access the file because it is being used by another process`, and the project's primary dev platform is Windows 11. Not caused by this change (it is inherent to holding a file open), but newly reachable now that a file destination exists. Distinct from the rotation entry above — that one is about unbounded growth, this one is about concurrent access and deletability. Mitigations worth weighing together: per-process log paths, a `QueueHandler` fan-in, or a locking rotating handler.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-39-swappable-log-destination.md`
  summary: `configure_logging()`'s `basicConfig(..., force=True)` closes and discards handlers owned by other parties on the root logger, which silently empties pytest's `caplog` for any future test that captures logs around `create_app()` or the CLI's `main()`.
  evidence: Proven during review with a probe test: `caplog.at_level(DEBUG)` followed by `configure_logging()` yields `caplog.text == ''` — the assertion fails with no error and no warning, which reads as "the code didn't log" rather than "the capture was destroyed". Latent today only because `tests/api/conftest.py` imports the app at collection time, before any per-test capture handler exists, so no current test is affected. The docstring now warns about it; a durable fix (removing foreign handlers without closing them, or skipping reconfiguration when pytest is active) is a change to the idempotency strategy and should be decided deliberately rather than patched in mid-story.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-39-swappable-log-destination.md`
  summary: The uvicorn-logger capture depends on uvicorn configuring its logging *before* it imports the app; calling `uvicorn.run(app_object)` with an already-imported app inverts that order, so uvicorn's `dictConfig` re-installs `propagate=False` and server logs silently stop reaching the configured destination with no error.
  evidence: Verified during round-2 adversarial review. uvicorn 0.51's `Config.__init__` calls `configure_logging()` before `Config.load()` imports the app, so the documented `uvicorn newsagent.api.main:app` form is safe — and that is the only form the README and infra use today. The failure is silent rather than loud, which is what makes it worth recording: a future change to how the API is launched (an embedded `uvicorn.run(app)` in a script, or a test harness) would quietly lose server logs. A durable fix would re-point the uvicorn loggers from a FastAPI startup hook rather than at import, or ship a uvicorn `--log-config` from news-agent-infra.

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-39-swappable-log-destination.md`
  summary: `NEWSAGENT_LOG_LEVEL` silently overrides an explicit `uvicorn --log-level` flag, because `configure_logging()` resets the uvicorn loggers to `NOTSET` so they inherit the configured root level.
  evidence: Surfaced by round-2 edge-case review and accepted deliberately: the alternative (only reset when the logger sits at uvicorn's INFO default) infers operator intent from a value that is also a legitimate explicit choice, which is worse than a documented rule. The chosen rule is single-source-of-truth — `NEWSAGENT_LOG_*` governs verbosity for the whole process — and it matches the user's stated intent that one wrapper capture everything worth watching in a deployment. Distinct from `--no-access-log`, which *is* honored, because that flag says "do not produce these records at all" rather than "how verbose". Worth revisiting only if news-agent-infra ends up wanting per-component verbosity; that would need a real logger-level map, not a second global flag.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-39-swappable-log-destination.md`
  summary: `httpx`/`httpcore` are pinned to a WARNING floor with no config escape, so an operator genuinely debugging a transport failure cannot get transport DEBUG without editing `logging_setup.py` — the exact thing GH #39 exists to eliminate.
  evidence: The pin was added at the user's explicit request so `LOG_LEVEL=DEBUG` surfaces raw LLM responses for GH #38 instead of per-connection traces, and it is genuinely the common case. The escape hatch needs a new setting (e.g. `NEWSAGENT_LOG_INCLUDE_TRANSPORT`), which the spec's Boundaries put behind "Ask First", so it was not added unilaterally mid-story. Note the current code uses `max(level, WARNING)`, so a stricter level still wins — only the *louder* direction is blocked. Cheap to add whenever a transport bug actually needs it.

- source_spec: none
  summary: Tests that assert code-level defaults read the `settings` singleton, which loads the developer's local `.env` first — 5 tests fail on this machine today for that reason, and the class keeps recurring (this is the second write-up).
  evidence: Already recorded on 2026-07-28 for `NEWSAGENT_SUGGESTION_PROVIDER`; re-confirmed 2026-08-06 with `test_default_provider_is_mock`, `test_default_provider_is_popularity`, and 3 tests in `tests/api/routers/test_me.py` failing on a clean checkout with no code changes. GH #39 makes it worse in principle by adding three more settings that `README.md` actively instructs developers to put in `.env` — `tests/test_logging_setup.py` defends itself with an explicit `default_settings` fixture, but that is a per-file workaround, not a fix. The systemic fix is a root `tests/conftest.py` autouse fixture that clears `NEWSAGENT_*` (and the unprefixed `EXTERNAL_LLM_*`/`LOCAL_LLM_*`) from the environment for the whole session, so pytest results never depend on local machine state.

## Deferred from: live manual testing of spec-topic-suggestions (2026-07-31)

- source_spec: `_bmad-output/implementation-artifacts/spec-topic-suggestions.md`
  summary: `_compute_and_store_suggestions` (`services/profile.py`) calls `suggest_topics` and `suggest_new_topics` sequentially — measured ~25s combined against the real configured OpenRouter model — so Step 3 shows "Finding suggestions for you…" for up to ~25-45s. GitHub issue: MONS-Designers/news-agent#36.
  evidence: Confirmed live: the two calls independently took ~11.4s and ~14.0s (~25.4s total) against `z-ai/glm-5.2` via OpenRouter. The immediate symptom (a too-short frontend poll budget silently falling back to "current subscriptions," making the feature look broken) was already patched in `TopicsStep.vue` (poll budget widened ~8s -> ~45s) — but the underlying latency itself is untouched and still a poor first-run experience even though it now resolves correctly instead of timing out. Neither call depends on the other's output, so they're a candidate for parallelization (e.g. `asyncio.gather` if the suggestion-source interface moves async, or a `ThreadPoolExecutor` given the current synchronous `SuggestionSource` contract) rather than a UI-only fix — out of scope to fix inline during a bug report, flagged for a dedicated pass.

## Deferred from: code review of spec-topic-suggestions (2026-07-31)

- source_spec: `_bmad-output/implementation-artifacts/spec-topic-suggestions.md`
  summary: `suggest_new_topics`'s de-dup context (`existing_topic_names`, built from `_topic_popularity`'s approved-only rows) never includes other users' still-`pending` invented Topic names, so the LLM can (and, given no cross-user memory, likely will) re-invent a near-duplicate name — including a case/whitespace variant that `add_topic`'s exact-match get-or-create won't catch either — creating multiple pending rows for what a human would recognize as one topic.
  evidence: This is the direct, spec-endorsed consequence of the spec's own explicit constraint ("`_topic_popularity`'s candidate list... filters to `status='approved'` only") — confirmed with the user via `bmad-spec`, not an implementation deviation. It mirrors the already-deferred 2026-07-28 entry below for Role-suggestion near-duplicate flooding (`PendingTaxonomySuggestion`); worth a joint fix (e.g. broaden de-dup context, or fuzzy-match at admin-review time) once the deferred admin approve/reject UI (CAP-4, entry above) exists to actually merge or reject duplicates.
- source_spec: `_bmad-output/implementation-artifacts/spec-topic-suggestions.md`
  summary: `services/sources.py::add_topic`'s select-then-insert-then-commit is now reachable from concurrent, user-facing "Save preferences" requests (via `services/preferences.py::set_preferences`) for the first time — two users (or a double-submit) picking the identical new topic name at the same moment can both pass the existence check before either commits, so the loser's commit raises an unhandled `IntegrityError` (`api/routers/me.py`'s `update_my_preferences` only catches `TopicCapExceededError`/`ValueError`), surfacing as a 500.
  evidence: This is the exact pre-existing TOCTOU pattern already flagged in the 2026-07-26 entry below for `add_field` ("faithfully mirrors `add_topic`/`add_source`... worth fixing project-wide rather than in one story"), previously only reachable from single-threaded admin/seed paths. This spec's own Boundaries explicitly says new-Topic creation "reuses `add_topic`'s existing get-or-create-by-exact-name idempotency unchanged" — fixing the race here would be an unscoped deviation; the project-wide fix (e.g. `IntegrityError` retry, matching `services/profile.py::save_profile`'s existing retry-on-conflict pattern for "Other" Field/Role text) should land in the same project-wide pass as the `add_field` fix.
- source_spec: `_bmad-output/implementation-artifacts/spec-topic-suggestions.md`
  summary: `services/profile.py::_compute_and_store_suggestions` never de-duplicates `suggestions` (the existing-Topic ranking result from `suggest_topics`) before truncating to `TOPIC_SUGGESTION_CAP` and storing as `suggested_topic_ids` — if the ranking source ever returns a repeated `topic_id`, nothing collapses it before `TopicsStep.vue` renders one chip per id.
  evidence: Pre-existing gap (predates this story — the ranking path itself is unchanged), confirmed by code review as not newly introduced, but this story's new merged-chip rendering path is what would first make a duplicate-id response visible as a Vue `v-for` duplicate-key rendering bug rather than a silent no-op. Low likelihood with the current prompt/candidate-filtering in `suggestions/llm.py`, but worth a defensive `dict.fromkeys`-style de-dup whenever `suggest_topics`'s contract is next touched.

## Deferred from: spec-topic-suggestions token-budget split (2026-07-30)

- source_spec: `_bmad-output/implementation-artifacts/spec-topic-suggestions.md`
  summary: Admin approve/reject UI for pending Topics (new `admin_topics.py` router mirroring `admin.py`'s Source-approval shape, plus `TopicQueueView.vue` mirroring `TaxonomyQueueView.vue`, plus nav/router wiring) — CAP-4 of the topic-suggestions feature.
  evidence: The full topic-suggestions spec measured ~3550 tokens, more than double the 1600-token soft ceiling. Split by the user's own choice: the primary user-facing capability (merged existing+invented Topic suggestions, immediate-but-pending creation on save, approved-only visibility to others) is safe and correct to ship without an admin UI — pending Topics simply accumulate until this follow-up ships, never leaking to other users in the meantime. This deferred piece is small and low-risk (near copy of the existing `admin.py`/`AdminView.vue` Source-approval pattern) and should become its own quick-dev spec once the primary piece is done.

## Deferred from: intent clarification for LLM-backed Role/Topic/Prompt suggestion wiring (2026-07-28)

- source_spec: none
  summary: Wire real Topic suggestions with support for brand-new (not-yet-curated) Topic names the LLM invents, distinct from Role's "Other" flow because Topic selection is a hard FK (`UserTopicPreference.topic_id`) rather than a free string.
  evidence: Confirmed design in conversation with the user (not yet spec'd): add a `status` column to `Topic` (pending/approved/rejected, mirroring `Source.status`'s existing shape). Selecting an LLM-invented new Topic creates a real `Topic` row immediately with `status='pending'` plus a real `UserTopicPreference` for that user right away (counts toward their cap, fully functional for that user immediately — unlike Field/Role's `PendingTaxonomySuggestion`, which defers row creation until admin promotion). Only `status='approved'` Topics are offered as suggestion candidates to *other* users. Admin approving flips status to `approved` (now suggestible to others); admin rejecting flips it to `rejected` and it stays stuck there permanently — still fully functional for the original selecting user, never offered to anyone else again. Needs: Alembic migration (existing Topics default `approved`), `services/sources.py` changes (`add_topic` gains a status param), a new service function mirroring `set_source_status` for topics, an admin approve/reject endpoint + UI (new, or extend the existing Taxonomy Curation Queue), and `suggest_topics`'s candidate list (`_topic_popularity` in `services/profile.py`) filtered to `approved` only plus now carrying `Topic.name` (today it's `topic_id`+`selection_count` only, no name — the LLM can't reason about topics it can't see the name of).

## Deferred from: code review of story-1.1 (2026-07-26)

- **No `GET /me/profile`; picker state dies on unmount.** `FieldStep` owns `selectedName`/`isOther`/`otherText` and is destroyed by `v-if`, and there is no read endpoint to rehydrate a saved `field_name`. A returning user sees a blank picker with no sign anything was saved; clicking Reload mid-entry silently discards typed input. Story 1.3's acceptance criterion ("Given I click Back from Step 2 … my previously selected Field/Role/Experience still show as selected") requires lifting this state into the shell plus a read path — that story owns the fix.
- **No Back navigation.** Clicking Continue advances to placeholder steps 2/3 with no way back short of a page reload. EXPERIENCE.md § Component Patterns requires Back on every step, always available and never gated, but steps 2/3 are intentional stubs in Story 1.1 — Back becomes meaningful when Story 1.4 builds Step 2.
- **Not responsive.** No media queries in either picker component; the three-item `flex: 1` stepper with `white-space: nowrap` labels overflows below ~375px, and nested 30px paddings compound on narrow viewports. Tracked as news-agent#30.
- **`add_field` TOCTOU race.** SELECT-then-INSERT against the unique `ix_fields_name`, committing once per field, so a concurrent seeder's loser raises an uncaught `IntegrityError` and aborts `seed_default_fields` mid-loop with a failed session. Pre-existing pattern: faithfully mirrors `add_topic`/`add_source` in `services/sources.py`. Worth fixing project-wide rather than in one story.
- **Editable install points at a stale worktree.** `pip show newsagent` resolves to `C:\project\news-agent\.claude\worktrees\github-issues-review-6e1f99`, so `python -m newsagent.cli` and `alembic` run another branch's code unless `PYTHONPATH=src` is set explicitly. Pre-existing environment issue, not caused by this change, but it will bite anyone running migrations from a plain shell.

## Deferred from: code review of spec-role-and-prompt-suggestions (2026-07-28)

- source_spec: `_bmad-output/implementation-artifacts/spec-role-and-prompt-suggestions.md`
  summary: The Role picker's `GET /me/fields/{field_id}/roles` fetch is a synchronous, in-request LLM call with worst-case ~90s of blocked FastAPI worker time (3 retries × 30s HTTP timeout + backoff) and no client-side abort/timeout, so a slow or hanging local LLM stalls Step 1 of the profile wizard.
  evidence: `suggestions/base.py`'s `_run` retries transient `SuggestionError`s up to 3 times with exponential backoff; `http_llm_client.py` defaults each attempt to a 30s timeout; `frontend/src/api/client.ts`'s `request()` has no `AbortController`. This is an emergent consequence of the story's own explicit "synchronously" / "no streaming or async" constraint, so it isn't this story's fix to make — worth a deliberate revisit (background polling, a shorter timeout, or a client-side abort) once a real local LLM is wired into a user-facing environment.
- source_spec: `_bmad-output/implementation-artifacts/spec-role-and-prompt-suggestions.md`
  summary: `services/taxonomy.py::suggest_roles_for_field` and `services/profile.py::suggest_prompts_for_user` only catch `SuggestionError`; a misconfigured provider (`get_suggestion_source()` raising `ValueError` for an unknown `NEWSAGENT_SUGGESTION_PROVIDER`, or an unconfigured `LOCAL_LLM_BASE_URL`/`LOCAL_LLM_MODEL`) propagates as an uncaught 500 instead of the "never blocks Role selection / no error shown" fallback the story promises.
  evidence: `suggestions/factory.py::get_suggestion_source` and `LLMSuggestionSource.__init__` both raise plain `ValueError` on misconfiguration, and it happens inside the `try: ... except SuggestionError` block in both new call sites. The same gap already exists in `_compute_and_store_suggestions` (pre-existing, but silent there since it's a `BackgroundTask`) — this story just adds two new *synchronous* call sites where it reaches the user as a 500. A systemic fix (base `SuggestionSource` config validated at startup, or `get_suggestion_source()` mapping config errors to `SuggestionError`) is out of scope for this story.
- source_spec: `_bmad-output/implementation-artifacts/spec-role-and-prompt-suggestions.md`
  summary: Once a Field accumulates more than `ROLE_SUGGESTION_CAP` (10) curated Roles, any curated Role alphabetically past the 10th becomes permanently unreachable in the picker — no pagination, recency, or relevance weighting, just a name-sorted truncation.
  evidence: `suggest_roles_for_field` does `curated[:ROLE_SUGGESTION_CAP]` off `list_roles`, which orders by `Role.name`. This matches the story's own spec'd cap behavior exactly (not a bug in this diff), but is worth a forward-looking note for whenever a Field's curated list naturally grows past 10 as the admin queue does its job.
- source_spec: `_bmad-output/implementation-artifacts/spec-role-and-prompt-suggestions.md`
  summary: No caching of LLM-generated Role/Prompt suggestions — every Field (re)selection and every Interests-step mount fires a fresh LLM call, and since the model has no reason to invent the same "new" role names twice, this is likely to flood `PendingTaxonomySuggestion` with near-duplicate, low-signal role names as usage scales, increasing admin curation burden.
  evidence: `suggest_roles_for_field`/`suggest_prompts_for_user` have no memoization keyed on Field/Role/Experience, and a user-selected new Role queues into the pending-suggestion table on every distinct wording the LLM happens to invent. Direct emergent consequence of making LLM-invented roles one-click-selectable; out of scope for this story to fix but worth watching once real users hit it.
- source_spec: none
  summary: The local `.env` file currently pins `NEWSAGENT_SUGGESTION_PROVIDER="llm"` (left over from this story's own manual verification step), which silently overrides test defaults and made 4 backend tests fail in a way that looked like a regression until traced to environment state rather than code.
  evidence: `tests/api/routers/test_me.py::test_get_roles_is_scoped_to_the_field` and 3 others assert against the code-level default (`popularity`), but pydantic-settings loads `.env` ahead of that default, so any local `.env` override silently breaks tests that don't explicitly monkeypatch the provider (confirmed: forcing `NEWSAGENT_SUGGESTION_PROVIDER=popularity` for the pytest invocation makes all 149 tests pass). Not a code defect, but worth deciding whether tests should force-isolate env vars (e.g. an autouse fixture) so a developer's local `.env` can never affect `pytest` results.
- source_spec: `_bmad-output/implementation-artifacts/spec-role-and-prompt-suggestions.md`
  summary: `except SuggestionError` in `services/taxonomy.py::suggest_roles_for_field` and `services/profile.py::suggest_prompts_for_user` swallows every failure (transient, provider, and malformed-output errors alike) with no logging, so a broken local LLM degrades invisibly in production with no signal for anyone to notice.
  evidence: Neither new `except` block logs anything before returning the curated-only/empty fallback. This mirrors the pre-existing, equally silent `except SuggestionError` in `_compute_and_store_suggestions`, so it's a systemic gap in `suggestions/` error handling rather than something new to this story — but this story extends the pattern into two more, synchronous, user-facing paths, which makes it more likely a real outage goes unnoticed. Worth a project-wide logging pass across all three call sites rather than a one-off fix here.
- source_spec: `_bmad-output/implementation-artifacts/spec-role-and-prompt-suggestions.md`
  summary: `ChipRow.vue`'s new chip-selection branching (`selectCurated`/`selectNew`/`selectOther`/`isChipSelected`) and `InterestsStep.vue`'s prompt-fetch/click flow have zero automated test coverage.
  evidence: The repo has no frontend test infrastructure at all (no `*.spec.ts`/`*.test.ts` files, no test script in `frontend/package.json`) — pre-existing, not introduced by this story. This story does meaningfully raise `ChipRow.vue`'s state-machine complexity (adding the curated-vs-new distinction) with nothing verifying it beyond manual testing; standing up frontend component testing is a separate, cross-cutting decision outside this story's scope.

## Deferred from: code review of spec-32-33-llm-provider-adapters (2026-07-28)

- source_spec: `_bmad-output/implementation-artifacts/spec-32-33-llm-provider-adapters.md`
  summary: `newsagent.suggestions` has no `Refusal`-equivalent outcome, so `LLMSuggestionSource` has no cheap way to decline degenerate input (blank `field_name`, empty `popularity`) before hitting the real network/paid endpoint.
  evidence: `suggestions/types.py` and `suggestions/base.py` were designed before any adapter made a real network call — `PopularitySuggestionSource` never needed to decline anything, so the gap was free until now. Adding it is an interface change (`SuggestionSource`/`SuggestionSuggestion` contract) out of scope for this story; a future story should decide whether to mirror `llm/`'s `Refusal` type or take a different approach.

## Deferred from: code review of spec-gh-25-weighted-digest-ranking (2026-07-31)

- source_spec: `_bmad-output/implementation-artifacts/spec-gh-25-weighted-digest-ranking.md`
  summary: The 5 ranking weights (`relevance_weight`/`recency_weight`/`interest_weight`/`interestingness_weight`/`personalization_weight`) have no startup validation that either weight trio sums to 1.0, despite the adjacent config comment asserting they do.
  evidence: A misconfigured `.env` (e.g. an operator tuning one weight without adjusting the others) silently produces a `final_score` outside its documented ~0-1 range with no error anywhere — surfaced by adversarial review of the ranking engine. Fixing it (a Pydantic `model_validator`) is a config-layer change orthogonal to this story's ranking logic; no other `Settings` field in `config.py` has cross-field validation today, so this is a broader config-robustness decision, not a one-off patch.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-25-weighted-digest-ranking.md`
  summary: `ranking.topic_affinity` re-scans and re-joins a user's entire past-sent-digest history (`Digest`→`DigestArticle`→`Article`→`Source`, lazy-loaded per row, no `selectinload`) on every single `build_digests` call, with no time-window or caching.
  evidence: Surfaced by adversarial review as an N+1 query pattern. Harmless at current MVP scale (2 dogfood users, short history), but cost grows unbounded with each additional sent digest and each additional user — worth an eager-load/time-window pass before scaling beyond dogfooding, not before.
- source_spec: `_bmad-output/implementation-artifacts/spec-gh-25-weighted-digest-ranking.md`
  summary: Articles that keep losing the top-N ranking never reach a terminal state — they stay "undelivered" indefinitely and get re-scored on every future run, so the candidate backlog only grows over the life of the deployment with no staleness cutoff or pruning policy.
  evidence: This is a new, real consequence of GH #25's cap (before this story, every undelivered article was attached and thus left the backlog every run); GH #25's own issue text is silent on backlog management, so no pruning policy could be inferred without guessing. Worth a deliberate policy decision (e.g. drop candidates past some age from ranking) once real usage data shows whether backlog growth is actually a problem.
