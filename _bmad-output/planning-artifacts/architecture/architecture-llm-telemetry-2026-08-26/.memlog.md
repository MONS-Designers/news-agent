---
scope: Per-call LLM telemetry: one durable record per LLM invocation across every caller (pipeline stages, digest voice, profile suggestions), carrying operation kind, FK to the produced result, latency, token counts, and a write-time cost snapshot priced from per-model env config.
purpose: build-substrate
altitude: feature
updated: 2026-08-26T23:36
---

- (constraint) INHERITED AD-1 (spine architecture-news-agent-2026-07-22, [ADOPTED]): thin-router / domain-service layering. Routers depend only on services; services take (Session, plain args); models carry no behavior. Binding, read-only.
- (constraint) INHERITED AD-3 (same spine, [ADOPTED]): llm/ and suggestions/ are SIBLING pluggable-provider packages selected by independent settings (NEWSAGENT_LLM_PROVIDER vs NEWSAGENT_SUGGESTION_PROVIDER). Neither imports the other. Providers never touch the DB directly - the calling service passes plain args. Binding: any telemetry design must not make either package import the other, nor make a provider a DB writer.
- (constraint) INHERITED AD-4 (same spine, [ADOPTED]): Alembic is the only schema-change mechanism. New tables ship as a new revision off the current head.
- (constraint) INHERITED convention (same spine): new settings are Settings fields with the NEWSAGENT_ prefix. NOTE contradiction with real code - external_llm_*/local_llm_* use UNPREFIXED aliases (EXTERNAL_LLM_BASE_URL etc). Pricing config sits in that same family, so the unprefixed precedent likely wins; flag to user.
- (direction) This spine picks up a Deferred item named verbatim in the parent spine: whether infra's LLM cost-control must treat the suggestion-source model as a second, distinct cost path from the article-generation LLM. Answer is now forced, not optional.
- (decision) USER-SETTLED: the record identifies the OPERATION KIND plus an FK to the produced result (not raw prompt text). Prompt bodies are not stored.
- (decision) USER-SETTLED: cost is computed and snapshotted AT WRITE TIME, so a later price change never rewrites history.
- (decision) USER-SETTLED: pricing is a per-model MAPPING from env config, not a single scalar rate.
- (constraint) USER-SET: must be generic - this feature is one piece of a larger system, so the design is judged on whether a future LLM caller is covered by construction rather than by remembering to instrument it.
- (constraint) CODE REALITY: http_llm_client.send_chat_completion is the single HTTP chokepoint - BOTH llm/external.py and suggestions/llm.py call it. Its module docstring declares it 'Domain-free: no article/topic/suggestion language, no error wrapping'. A DB write placed there would break that stated contract.
- (constraint) CODE REALITY: usage today is an in-memory list on LLMProvider (_record_usage/drain_usage), drained per-article by pipeline/relevance.py and pipeline/summarize.py into a per-run aggregate. Nothing per-call is ever persisted. suggestions/llm.py passes no on_usage callback at all - zero accounting. pipeline/digest.py never drains, so compose_digest_voice LLM spend is entirely invisible.
- (constraint) CODE REALITY (WIP, uncommitted): src/newsagent/scheduler.py is a NEW second caller of the pipeline - calls get_llm_provider() and digest_stage.build_digests() every 120s and never calls pipeline_runs.record_run(). The GH-19 spec explicitly accepted this as a known tradeoff for 'a future non-CLI caller'; that caller now exists, so scheduler-driven spend will be invisible in production.
- (constraint) CODE REALITY: pipeline_runs table is per-CLI-invocation aggregate only (run_type, counts, usage_input_units, usage_output_units). No cost, no latency, no model, no user, no per-call rows. GH-19 spec froze 'No cost/currency conversion - tokens only' as an explicit Never at the time.
- (constraint) CODE REALITY: Usage dataclass is deliberately unit-neutral (unit='words' default, 'tokens' when the provider reports them) - llm/types.py docstring: 'token counts are an LLM assumption and are deliberately not part of the contract'. MockLLMProvider emits unit='words'. Any cost calculation must therefore handle a non-token unit rather than assume tokens.
- (constraint) WIP diff check: no uncommitted change touches usage accounting, http_llm_client.py, or llm/base.py. The open llm/* diffs are prompt/content only (bullets_he -> paragraphs_he, Hebrew output rule). No collision with this work.
- (event) Coaching path selected. Deliverables: spine + infra boundary-contract doc + visual explainer. Workspace held in scratchpad until the user commits their WIP.
- (decision) USER-SETTLED: growth is BOTH inward (more LLM operation kinds) AND outward (non-LLM outbound work - email sends, RSS fetches, any external call). Consequence: the record is NOT llm-shaped. Cost/tokens must be optional dimensions of a generic outbound-call record, not required columns. This reopens the table name and the pricing model's placement.
- (question) OPEN (blocking, user asked for a visual before deciding): which seam carries the measurement, and how does caller-side identity reach it. User could not evaluate the options without seeing the current call hierarchy laid out.
- (event) Working tree now clean at dev/50ce4d8, matching origin/dev - nothing unpushed. Write-hold on _bmad-output is lifted.
- (constraint) VERIFIED (committed state, not WIP): scheduler.py is tracked, introduced by commit 52e48e3 'feat: standing delivery scheduler with per-user cadence and a DB-backed lease', and contains ZERO occurrences of record_run. The accounting gap is live on dev, not a pending change. Upgrades the problem from 'about to regress' to 'already regressed'.
- (question) OPEN (new, raised by the commit state): whether to ship a stopgap record_run() call in scheduler.py NOW, independent of this spine, to stop the bleeding while the generic design is built. Cheap and reversible, but adds a call site the spine intends to delete.
- (decision) USER-SETTLED: the artifact and all downstream documents are written in HEBREW, RTL. Overrides bmm config document_output_language=English and diverges from the existing English artifacts under _bmad-output/.
- (constraint) VERIFIED: pipeline/digest.py contains zero references to usage or drain_usage; DigestReport has only users_processed/digests_created/articles_added. compose_digest_voice is called at digest.py:112. Since scheduler.run() builds one provider and reuses it across every 120s tick, LLMProvider._usage_log grows without bound for the process lifetime - nothing in the digest path ever drains it. Low severity by volume, but genuinely unbounded, and it is direct evidence that correctness depends on every caller remembering to drain.
- (decision) REJECTED (user push-back, correct): the record_run stopgap in scheduler.py. It is not 3 lines - it needs draining in the digest path, usage fields on DigestReport, a new RUN_TYPE_DIGEST constant, and the record_run call. Every one of those four is option-C per-call-site opt-in, i.e. MORE of the pattern this spine intends to remove, plus a new run_type value that lands in table data and would then need migrating.
- (question) NEW AD REQUIRED (surfaced by the user asking whether the spine should cover this): who owns the single answer to 'what did we spend'. Once per-call records exist, pipeline_runs + drain_usage are a second independently-computed answer to the same question - two owners of one truth, exactly the divergence a spine prevents.
- (constraint) CONSTRAINT on that AD: pipeline_runs cannot become a purely derived view. _refuse_if_junk returns a Refusal BEFORE _request, so a refused article makes no HTTP call and therefore produces no telemetry row. Refused/error counts have no other home and cannot be reconstructed from per-call spend records.
- (direction) LEAN on that AD: per-call table becomes the sole owner of spend and latency; pipeline_runs keeps run-outcome counts and DROPS its two usage columns; drain_usage/_usage_log are deleted, which removes the unbounded-growth bug for free. One owner per fact, small migration, no run_type value invented.
- (decision) USER-SETTLED: 'what did we spend' is required at BOTH levels - per single call AND per pipeline run. Resolves the ownership AD: calls are the atom and own spend+latency; a run row owns outcome counts (including refusals, which never make a call) and calls carry a run FK. Pipeline-level spend is an aggregation over calls, never a separately maintained number.
- (decision) USER-SETTLED: authorized to REBUILD the accounting mechanism from scratch if it simplifies the architecture. pipeline_runs, drain_usage and _usage_log are no longer fixed constraints - they may be replaced. This removes the backward-compatibility pressure that shaped the earlier options.
- (feedback) USER: the explainer artifact was too long and disorganised, and did not make the actual open problem legible. Four figures to ask one question. Corrective: state the problem in three sentences, one figure, one question.
- (direction) REFRAME after the rebuild permission: option 3 (bubble measurement up) is stronger than drawn, because drain_usage leaked as a SIDE CHANNEL that could be silently ignored. Rebuilt as part of the provider's RETURN TYPE it becomes visible at every call site. The open fork therefore collapses from 3x3 to two: (A) ambient context written by the transport, vs (B) measurement as part of the returned value, recorded by the caller.
- (constraint) USER-SCOPED: 'rebuild' covers ONLY the meter mechanism and its DB persistence - pipeline_runs table + services/pipeline_runs.py, the Usage type, _usage_log/_record_usage/drain_usage in llm/base.py, the usage fields and _accumulate_usage helpers on FilterReport/SummarizeReport, the record_run calls in cli.py, and the usage-report CLI command. NOT in scope: provider result contracts (RelevanceScore/SummaryResult/DigestVoice), pipeline stage structure, http_llm_client's design, or AD-3's sibling split.
- (decision) USER-SETTLED (resolves the blocking fork): the worrying scenario is a developer adding a caller without knowing the telemetry exists. Selects option A - ambient context. Coverage-by-construction beats call-site visibility.
- (decision) Option B (measurement as part of the returned value) is ALSO ruled out independently by the narrowed rebuild scope: it would require changing the frozen provider result dataclasses and every call site, which the user placed out of scope. Both of the user's answers point the same way.
- (direction) Resulting shape under the narrowed scope: http_llm_client keeps its domain-free contract and gains only measurement REPORTING (elapsed time alongside the tokens it already reports via on_usage) - it never writes to the DB and never learns what the call was for. A new meter module reads the ambient context, composes the record, and a service writes it. Transport logic untouched.
- (question) HONEST BOUNDARY to state in the spine: ambient context guarantees coverage for anyone using the project's shared transport. A future developer who writes their own HTTP call to an LLM, bypassing send_chat_completion entirely, is uninstrumented under ANY of the designs considered. Do not overclaim.
- (question) REMAINING BLOCKER for the schema: one row per HTTP attempt (retries each get a row, sharing an operation id) vs one row per logical operation (retries folded in). Affects row volume and how cost-per-article is queried.
- (decision) USER-SETTLED (record shape): parent record aggregates 'user X wanted Y' with totals - tokens in A, tokens out B, total tokens A+B, price USD Z, duration ms F. Child record per LLM call carries: purpose (e.g. FILTERING), output, tokens in, tokens out, total tokens, duration ms, price USD, model. Pipeline runs get a parent; a lone call (suggestions, roles) records its INPUT SUMMARY instead - enough to understand what the user wanted, never the full prompt.
- (constraint) VERIFIED BLOCKER on the parent record: filter_pending_articles and summarize_relevant_articles have NO user dimension. filter touches UserTopicPreference only to skip topics with zero subscribers; summarize.py mentions 'user' nowhere. Only build_digests is per-user. So the cost of summarizing one article is SHARED across every subscriber whose digest includes it - 'user X wanted Y, total cost Z' is not answerable for the filter/summarize stages without an explicit allocation rule.
- (question) BLOCKING: how is shared pipeline cost attributed? Options: (a) parent is per-RUN not per-user for shared stages, and per-user only for digest build; (b) split each article's cost across its subscribers; (c) attribute to no user and accept that per-user cost covers only the digest stage.
- (direction) PUSH-BACK to raise: making the parent record CONDITIONAL on call count is a divergence risk - 'what the user wanted' would live in two different places depending on shape, and a single-call operation that later grows a second call changes shape retroactively. Recommend always creating the parent; the input summary she wants for lone calls IS the parent's 'what was wanted' field.
- (direction) OUTPUT-LENGTH question (she flagged it): outputs here are small - external.py's own comment records measured summarize responses at 531-721 chars, and filter output is {"score": x}. Reuse the existing _clip precedent (head+tail window, _CONTENT_LOG_CHARS=500) rather than inventing a new truncation rule; store full length separately so truncation is visible.
- (question) RAISE: this table becomes a home for user free text (interest_free_text drives suggestion prompts) and article-derived output, with no retention policy. Retention/PII needs a spine decision, not a silent default.
- (decision) USER-SETTLED: option (a) - the parent record is PER RUN for the shared stages, carrying the subscriber count for that run when available, so cost-per-subscriber-per-run is directly computable. Only the digest build is per-user.
- (decision) USER-SETTLED: do NOT store the LLM output text. Store only the character count returned. Removes the truncation question and most of the output-side PII exposure.
- (direction) PURPOSE NAMED BY USER: the telemetry exists to answer whether the product is profitable and sensible. This reframes the design goal from accounting to unit economics - WASTE must be a first-class query, not archaeology.
- (constraint) VERIFIED waste paths that the FK-to-result makes measurable: (1) every scored article costs a filter call, and the irrelevant ones produced nothing; (2) summarize runs BEFORE digest selection while digest_max_articles=7 caps each digest, so articles are summarized and then never delivered; (3) max_summarize_attempts=3 means up to three billed attempts before a terminal 'failed'. Refusals are the free case - _refuse_if_junk returns before the network.
- (constraint) VERIFIED blind spot: digest.py:198 skips _compose_voice entirely when _reuse_recent_voice succeeds (commit 464c154, matched on the exact article set). A cache hit therefore makes no call and would leave no row, so the hit rate - itself a profitability lever - is invisible. Recommend recording zero-cost rows for avoided calls.
- (direction) Storing raw token counts ALONGSIDE the snapshotted price is what makes model-switch what-ifs possible retroactively: replay historical token counts against a different rate. Snapshot = accounting truth; raw tokens = the lever. Both already decided, but the reason belongs in the spine.
- (constraint) SCOPE BOUNDARY for the spine: this measures MARGINAL cost only. The scheduler ticks every 120s regardless of load and infra/hosting cost is news-agent-infra's. 'Cost per user' from this table must never be read as total cost per user.
- (decision) USER-SETTLED: record avoided calls too - a cache hit (digest voice reuse) writes a zero-cost, zero-token row marked as avoided, so the cache hit rate is itself measurable.
- (decision) ADOPTED BY NON-OBJECTION (flag visibly in the spine so she can still catch it): the parent run record is created ALWAYS, including for a lone call, rather than conditionally on call count. Raised as a proposal, not objected to.
- (version) Alembic head verified as d4a7b2c85f16 (scheduler_lease) - the new revision's down_revision.
- (event) User instructed: write the spine.
- (version) requires-python = '>=3.12' verified in pyproject.toml, so contextvars (stdlib since 3.7) needs no dependency. No new packages required.
- (decision) REVIEWER GATE, adversarial lens, hole 1 CLOSED: AD-11 did not fix run granularity. Two builders could each obey it while one opened a run per stage invocation and the other per article. Fixed by pinning two nesting levels - open_run() once per stage invocation, attribute_call() per unit of work.
- (decision) REVIEWER GATE, hole 2 CLOSED: subscriber_count was undefined - 'all active users' vs 'subscribers of the topics this run touched'. Pinned to the latter in AD-13.
- (decision) REVIEWER GATE, hole 3 CLOSED: nothing said WHO sets attempt. Only llm/base.py:108 _run knows a call is a retry; the transport cannot know and the sink could only guess by counting prior rows, which races. AD-15 now pins the retry loop as the sole incrementer.
- (decision) REVIEWER GATE, hole 4 CLOSED: an avoided (cache-hit) row never passes through the transport, so AD-12's 'transport reports' rule left it unowned and looked contradicted. AD-15 now declares the caller as the sole reporter for avoided rows, and names it as the single exception.
- (event) lint_spine.py clean, 0 findings. Reviewer gate lenses run inline (no subagents, per session policy). Spine finalized.
- (constraint) USER-SET (cross-repo): ALL code lives in news-agent. news-agent-infra can ONLY schedule the execution of a command that this repo exposes. It runs nothing of its own. This invalidates the Deferred item that handed the weekly pricing refresh to infra.
- (constraint) VERIFIED: EXTERNAL_LLM_BASE_URL is empty in .env.example and the real endpoint is not pinned anywhere in the repo. OpenRouter appears only in a code comment in http_llm_client.py:88. Cannot assert the provider - whether an automatic pricing feed is even possible is a question for infra, not an assumption.
- (decision) AMENDS AD-16 (id kept stable, Rule amended in place): pricing is read at call time from a model_prices TABLE, never from an env var. LLM_PRICING_JSON demotes to an INPUT for the refresh command, used only when the provider exposes no pricing endpoint. One owner for the rate, and 'updated weekly' becomes a real mechanism instead of a human process.
- (decision) NEW AD-21: the cross-repo boundary is a CLI command plus its exit codes. news-agent exposes 'refresh-pricing'; infra schedules it weekly and observes only the exit code. Exit codes are therefore the entire API surface between the repos and must be specified.
- (event) Deliverables complete: ARCHITECTURE-SPINE.md (final, AD-11..AD-21), infra-boundary-contract.md, and the visual explainer artifact. Spine amended for the cross-repo constraint after it was raised. Nothing committed.
