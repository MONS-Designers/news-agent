---
scope: Epic A self-registration: whether/how to merge the Waitlist table into User via a status column, and how the registration-cap count stays correct either way
purpose: build-substrate
altitude: epic
updated: 2026-09-07T22:05
---

- (constraint) Inherited from architecture-news-agent-2026-07-22 (sibling feature spine, but its AD-1/AD-4 are project-wide conventions, not feature-specific): AD-1 thin-router/domain-service layering (routers never touch models, services own mutation+idempotency); AD-4 Alembic is the only schema-change mechanism, no create_all/hand DDL.
- (direction) Triggering input: sprint-change-proposal-2026-09-07.md section 3.2/4.4 (OQ4) - Nomi wants to merge the separate Waitlist table into User via a status column for single-table DB management and one-step promotion, but left the safety mechanism (how the cap-count query stays correct) as an open decision for this architecture run to work through with her, not predetermined.
- (question) Nomi asked directly: 'What's advisable? Maybe there's a different, perfect solution?' - i.e. is there an option beyond the 4 (DB check constraint / partial index / centralized counting function / keep two tables) already surfaced in the change proposal.
- (decision) Nomi's call: do NOT merge Waitlist into User now. The rowcount bug (sprint-change-proposal-2026-09-07.md section 1.1) proceeds to fix immediately, on the existing two-table design. The 'single person, one lifecycle' question (whether waitlist is really a status of User rather than a separate entity) is deferred to a new, separate task/issue, opened specifically to decide if it's even needed - not assumed yes.
- (event) Run paused, not finalized - no architectural invariant is being fixed in this run (the decision was explicitly not to change anything now). Resume this memlog if/when the new 'single lifecycle' task concludes that a merge is warranted; the 4+1 safety-mechanism options (DB check constraint / partial index / centralized counting function / keep two tables / promote-CLI keeping two tables) discussed in that prior conversation remain the starting menu for that future run.
