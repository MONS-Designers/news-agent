
## 2026-08-07 - GH #19 usage-accounting gap
Room's first session. Debated where to attach LLM token accounting when a call
retries or fails outright (base.py retries transient errors up to 3x; failed
calls still bill). Winston pushed back hard on Amelia's first instinct
(attaching usage to LLMError) - usage is accounting, not an error-hierarchy
concern. Landed on: a usage callback fired from send_chat_completion the
moment a response body carries a `usage` block, before choices/content is even
validated - independent of success. Nomi confirmed: tokens only, no cost/₪
conversion needed (Sally raised it, John/Nomi closed it). Paige flagged that
the "fires before validation, on purpose" ordering needs an inline comment or
it'll look like a bug to a future reader.

## 2026-08-07 - "can we stand it up, or will it embarrass us?"
Nomi asked point-blank. John refused the framing until "embarrass in front of
whom" was answered - with 2 seeded dogfood users, the word barely applies.
Amelia's "420 tests pass, the pipeline is built" ran straight into Winston's
"a passing suite is not a running system": the same friction as the #19 round,
now a standing bit between them. The hard stop nobody could argue with:
`mail/factory.py` only knows `console`, so nothing can be delivered at all -
"standing it up" was never on the table. Mary reframed with the measured 15%
summarize failure rate; Sally's counter landed the round - a failed summary is
invisible, so the real embarrassment is what *does* arrive (GH #37: seven
articles, one topic). Sally also called back her own ₪ question from #19: with
deterministic failures retried forever, cost stopped being theoretical.

Then Nomi answered John's question and it broke the room open: the audience is
NOT 2 seeded dogfooders - it's friends, technical and not, who want fast simple
signup and content that nails their interests. That is the self-registration
product CLAUDE.md's "Known drift to resolve" has flagged since day one, and
`api/auth.py::resolve_identity` returns None for any unseeded email, so a friend
literally cannot log in today. Mary's line: "the drift didn't get resolved, it
got overtaken." The round's real find was Amelia's: `relevance.py:81` and
`summarize.py:63` feed the model `rss_summary or title` - `full_text` is never
populated (GH #9 open), so "excellent content" is currently a model summarizing
a snippet, sometimes just a headline. Sally and Amelia split on whether #9 leads
the new epic or follows it; John warned the scope had just doubled. Winston broke
the tie by shrinking #9 to "fetch full text only for articles that already passed
relevance, soft-fall-back to rss_summary" - Amelia accepted it. Sally noted twice
in one session that Winston restates her point with a diagram and gets the credit
(running bit now).

OUTCOME: Nomi approved John's ordering - responsive/#30 as item 0, then
self-signup, real email sender, #9, #37, terminal-failure state + #19, #36 under
8s. Self-registration is OPEN but hard-capped at 10 users, not the invite list
John proposed - which leaves a question nobody in the room caught: what user #11
sees. Epic drafting handed to `bmad-create-epics-and-stories`.

## 2026-08-07 - epics-launch-readiness.md drafted; the "local model" mix-up
The room wrote a companion epic file rather than overwriting epics.md (Epic
1/2, shipped, stays untouched) - 10 FRs, 6 NFRs, and 3 open questions (OQ1
user-#11 experience, OQ2 relevance never sees full text, OQ3 the 8s budget).
Nomi asked point-blank: does a local model fix OQ3? Amelia caught the actual
trap before anyone answered: `suggestions/llm.py`'s `LOCAL_LLM_BASE_URL` is
just a config name distinguishing it from `external_llm_*` (AD-3) - it is NOT
hardware. #36's 11.4s/14.0s numbers were already measured against that
"local" adapter pointed at OpenRouter's z-ai/glm-5.2, a remote hosted model.
Winston's verdict: real on-hardware inference removes a network hop (small)
but not the compute time (the bulk of it) unless the model is meaningfully
smaller - and smaller trades suggestion quality for speed, a decision nobody
had made. Sally flagged concurrency alone lands at ~14s, still over budget.
Landed nowhere final - Nomi has the actual tradeoff now, decision pending.

## 2026-09-10 - "delete it mentally" - the comments fight
project-context.md generation. Nomi proposed "clean code is code without
comments". Paige refused it flat, with a callback to her own #19 line: the
fires-before-validation comment she demanded back then is alive in
`telemetry/sink.py` today, and a blanket rule would let someone delete it and
then "fix" the ordering. She dared Amelia to read one "monologue" comment aloud
from `services/profile.py` (116 comment lines). Amelia couldn't, and conceded -
first time she's folded to Paige. Sally split the term in two (a comment that
restates the code vs one that encodes a trap the code cannot say), Winston
immediately restated it as a rule, and Sally called him on it out loud - the
running bit from 2026-08-07 is now explicit between them, and he didn't deny it.
Mary closed it with evidence rather than opinion: Nomi's own global rules
already say match-existing-style and don't-improve-adjacent-code, so the
qualified form was never a compromise, it was what Nomi had already decided.
John's one question ("do you mean the existing code isn't clean?") is what
forced the new-code-only scope.

LANDED: default no comments, plus the delete-it-mentally test; WHY / traps /
AD-ids and issue numbers survive it; new code only; stripping existing comments
is an explicit task from Nomi, never a side effect.

Then Nomi corrected the room's factual premise - `.env` is the TEST Neon
branch, `.env.prod` is production - and it sharpened the rule instead of killing
it: `.env` carries live SMTP credentials and a real LLM token, so the actual
hazard of a careless local run is real email to real people and real money
spent, not a wiped database. Amelia had asserted the prod-DB version; Level
would have caught it, and nobody in this room did.

## 2026-09-10 - the read/write split, and "don't restate Nomi to Nomi"
Same session, second round. Nomi added three rules: no installing libraries
without approval, no git commands without approval, and "senior 10 years"
standards. Amelia caught the git one immediately as a live contradiction - she'd
run `git status` five minutes earlier and found a parallel session's staged
files, exactly as Nomi's own memory tells her to. Paige refused John's
either/or framing and split it read vs write in one line; Amelia agreed with
her twice in an hour and said out loud that it was disturbing. Paige: "get used
to it." The Paige/Amelia truce is now a thing. Winston generalized the same
split to libraries (installing declared deps is setup, not adding), then started
restating Sally again mid-sentence, and this time admitted it when she said his
name - Amelia demanded it be minuted.

Mary's find carried the round: the no-new-dependency rule isn't new policy, it's
already an unbroken habit - `mail/smtp.py` uses stdlib smtplib over a vendor
SDK, `http_llm_client.py` is a thin httpx transport instead of a provider SDK,
`profile-draft.ts` uses a module ref because Pinia isn't installed. Three
independent decisions, one instinct.

Paige's bigger point reframed the whole artifact: "senior 10 years" is already
written, twice, in Nomi's global CLAUDE.md as executable behavior - copying it
into project-context.md creates a second source of truth that will drift. Mary
turned that into a test for every future section: this file holds only what is
true ONLY here. That test now governs the rest of the run.

OUTCOME: all three accepted in qualified form. Nomi confirmed git = state-
changing commands only.

## 2026-09-10 - Sally turns the bit around, and Paige starts counting lines
Framework-rules round. Amelia opened by killing a rule with citations: the
"routers return domain objects" claim is wrong a third of the time -
`routers/admin.py:16` returns a raw ORM row, `routers/me.py`'s
`get_my_subscription` builds the schema by hand because a computed boolean has
no row to return. Paige extracted the actual rule from the exception in one
line. Winston then found the invisible thing under the per-item-commit rule:
`db.py:7`'s `expire_on_commit=False`, copied into five separate test
sessionmakers because without it tests diverge from production. Sally spelled
out the consequence, Winston said "that's exactly what I said" - and Sally
turned the running bit around on him on the spot ("you said the parameter name,
I said what happens"), Amelia demanded it be minuted, and Winston conceded that
one too. Second concession in two rounds; he's losing this bit.

Mary's find was the round's best: the project enforces per-table WRITE OWNERSHIP
and documents it in three independent docstrings (preferences.py:14 sole
mutation point for UserTopicPreference and the 4-topic cap, services/telemetry.py:3
sole writer of OutboundRun/OutboundCall, api/auth.py:51 only place a User row is
created pre-auth). Her argument for why it outranks everything else: an agent
doesn't break it in code it writes, it breaks it by not reading code it never
opens - tests pass, the cap just stops being enforced on one path. Sally added
the money version (summarize.py filters to subscribed topics before the paid
call, GH #45).

New tension, likely to define the rest of the run: Paige started measuring the
artifact instead of arguing about it - 180 lines at category 3 of 6, loaded into
every agent's context on every task, and she warned her own hard-won comments
rules will be buried by page three. She traded away the telemetry paragraph;
Winston refused to give up the alembic downgrade rule and won. Nomi has the
cut-vs-continue call.

## 2026-09-10/11 - project-context.md shipped, and Paige's line count won
Categories 4-6 ran without the room - Nomi drove them straight through on [C].
Two factual corrections from Nomi reshaped sections mid-flight: `.env` is the
TEST Neon branch (not prod), and sqlite stays for tests while everything that
isn't a test uses whatever `.env` resolves to. The second one arrived as "we're
done with sqlite" and 35 test files said otherwise - worth remembering that the
room's read of the repo and Nomi's read of the plan can diverge, and asking beat
guessing.

Final artifact: ~441 lines, 7 top-level sections, reorganized at the end because
append-order is not read-order. Paige's context-budget argument is now baked into
the file itself - the Usage section tells future humans that every line costs
context on every task and must earn its place, and tells agents that where the
file and the code disagree, the code wins and the file is the bug. That last line
is the room's real legacy from this session: Winston wanted invariants, Amelia
wanted citations, Paige wanted the thing to stay readable, and the tiebreaker
they all accepted was "the code is the fact".
